const { name } = require("ejs");

/*Variable are containers that store values.*/
let myVariable;
myVariable ="Bob"

/*String lengths and retrieve number of characters */
var name="Angela"
name.length;

var age = 20
console.log(age)
